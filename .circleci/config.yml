version: 2.1

jobs:
  build_and_test:
    executor: node/default
    steps:
      - checkout
      - node/install-packages:
          pkg-manager: npm
      - run:
          command: npm run lint:check
          name: Run linter
      - run:
          command: npm run build
          name: Build app
